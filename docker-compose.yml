services:
  leaderboard:
    build: .
    container_name: leaderboard
    ports:
      - "8080:8080"
    volumes:
      - leaderboard-data:/app/data
    restart: unless-stopped

volumes:
  leaderboard-data:
